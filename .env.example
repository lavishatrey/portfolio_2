# Example environment variables
# Set the Vite client API base (must start with VITE_ to be exposed to the client)
VITE_API_URL=http://localhost:4000/api

# Server-side environment (used by the server in the /server folder)
PORT=4000
DB_FILE=server/db.json
UPLOAD_DIR=server/uploads
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority
